<h1> Notes </h1>
  <%= link_to 'New Note', new_note_path %>
  <% @notes.each do |note| %>
    <h2><%= note.title %></h2>
  <%= link_to 'Show Note', note_path(note) %>
  <%= link_to 'Edit Note', edit_note_path(note) %>
  <%= link_to 'Destroy Note', note_path(note), method: :delete %>
<% end %>
